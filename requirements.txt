torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
pytest>=7.0.0
prometheus-client>=0.20.0
fastapi>=0.110.0
starlette>=0.36.0

# optional — enables Flash Attention 2 in GQAttention (requires CUDA + build tools)
# flash-attn>=2.8.3

# optional — enables Japanese morphological analysis in LLMOScorer (install either one)
# janome>=0.5.0
# fugashi>=1.3.0  (also requires: ipadic or unidic)
